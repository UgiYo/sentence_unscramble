# 英文句子拆解打亂工具

這是一個純前端靜態網站，可協助老師快速製作英文句子重組練習。

## 功能

- 輸入題數後自動建立對應數量的英文句子輸入欄位。
- 將每一句英文以空白拆成單字，並以 `/` 顯示打亂後的順序。
- 產生一行填空題目，保留一個提示字，其餘單字顯示為 `_____`。
- 標點符號會保留在原本相連的單字上，例如 `today.`。

## 使用方式

直接用瀏覽器開啟 `index.html`，或用任一靜態伺服器啟動專案：

```bash
python3 -m http.server 8000
```

接著開啟 <http://localhost:8000>。
